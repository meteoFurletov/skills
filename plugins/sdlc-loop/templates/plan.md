# Plan: <same subject as the spec>

From `spec.md` (<date>). Owner: <name>. Status: draft. Date: <YYYY-MM-DD>.

Requirements and design live in `spec.md` and are not repeated here.

## Scenarios this work proves

- `<path>.feature` — <scenario name>

<Or: "No scenario changes. The existing scenarios must still pass.">

## Approach

1. <step>

## Files that change

<Repo-relative paths, one per line, no prose. The `plan-sync` hook reads this
list, so it has to be real. A directory path covers everything beneath it.>

- <path/to/file>

## Risks and rollback

<What could go wrong, and how to undo it.>
